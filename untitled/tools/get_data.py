from tools import project_path
# import pandas as pd
from openpyxl import load_workbook

class GetData:
    headers = None
    account = None
    loan_id = None
    # NoRegTel =pd.read|_excel(project_path.test_case_path, sheet_name='init').ix[0,0] # 从Excel里面获取数据
    # normal_tel = pd.read|_excel(project_path.test_case_path, sheet_name='init').ix[1,0]
    # admin_tel = pd.read|_excel(project_path.test_case_path, sheet_name='init').ix[2,0]
    # loan_member_id = pd.read|_excel(project_path.test_case_path, sheet_name='init').ix[3,0]
    # admin_tel =
# setattr(GetCookie,'Cookie','123456') # 设置属性值
# print(hasattr(GetCookie,'Cookie'))
# print(getattr(GetCookie,'Cookie'))
# print(normal)
#     def get_datas(self,file_name,sheet_name):
#         wb = load_workbook(file_name)
#         sheet = wb[sheet_name]
#         NoRegTel =sheet.cell(0,0)
#         normal_tel = sheet.cell(1,0)
#         admin_tel = sheet.cell(2,0)



